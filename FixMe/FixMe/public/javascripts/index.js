var vars = {
    getPerson = require('../objects/person').getPerson,
    setPerson = require('../objects/person').setPerson(person)
}

document.addEventListener('', function () {
    //What do we do here?
});



